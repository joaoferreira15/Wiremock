{{- if .Values.spring-instance-local.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.spring-instance-local.ingress.name }}
  annotations:
    {{- if .Values.spring-instance-local.ingress.tlsEnabled }}
    # Ensure that the ingress controller is set up for TLS termination (using cert-manager or other mechanisms)
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    {{- else }}
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    {{- end }}
    kubernetes.io/ingress.class: nginx
spec:
  {{- if .Values.spring-instance-local.ingress.tlsEnabled }}
  tls:
    - hosts:
        - {{ .Values.spring-instance-local.ingress.host }}
      secretName: {{ .Values.spring-instance-local.ingress.tlsSecretName }} # The secret containing the TLS certificates
  {{- end }}
  rules:
    - host: {{ .Values.spring-instance-local.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.spring-instance-local.service.name }}
                port:
                  number: {{ .Values.spring-instance-local.service.ports.port }}
{{- end }}