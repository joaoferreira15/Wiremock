apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.spring-instance-local.service.name }}
  labels:
    app: {{ .Values.spring-instance-local.service.name }}
spec:
  type: {{ .Values.spring-instance-local.service.type }}
  ports:
    - port: {{ .Values.spring-instance-local.service.ports.port }}
      targetPort: {{ .Values.spring-instance-local.service.ports.targetPort }}
  selector:
    app: {{ .Values.spring-instance-local.deployment.name }}