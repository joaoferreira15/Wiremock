{{- if .Values.wiremock.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "wiremock.fullname" . }}
  labels:
    {{- include "wiremock.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "wiremock.fullname" . }}
  minReplicas: {{ .Values.wiremock.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.wiremock.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.wiremock.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.wiremock.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.wiremock.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.wiremock.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
