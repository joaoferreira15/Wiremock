{{- if .Values.spring_instance_local_2.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.spring_instance_local_2.ingress.name }}
  annotations:
    {{- if .Values.spring_instance_local_2.ingress.tlsEnabled }}
    # Ensure that the ingress controller is set up for TLS termination (using cert-manager or other mechanisms)
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    {{- else }}
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    {{- end }}
    kubernetes.io/ingress.class: nginx
spec:
  {{- if .Values.spring_instance_local_2.ingress.tlsEnabled }}
  tls:
    - hosts:
        - {{ .Values.spring_instance_local_2.ingress.host }}
      secretName: {{ .Values.spring_instance_local_2.ingress.tlsSecretName }} # The secret containing the TLS certificates
  {{- end }}
  rules:
    - host: {{ .Values.spring_instance_local_2.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.spring_instance_local_2.service.name }}
                port:
                  number: {{ .Values.spring_instance_local_2.service.ports.port }}
{{- end }}