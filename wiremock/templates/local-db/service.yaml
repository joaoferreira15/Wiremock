apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mysql-instance-local.service.name }}
  labels:
    app: {{ .Values.mysql-instance-local.service.name }}
spec:
  type: {{ .Values.mysql-instance-local.service.type }}
  ports:
    - port: {{ .Values.mysql-instance-local.service.ports.port }}
      targetPort: {{ .Values.mysql-instance-local.service.ports.targetPort }}
  selector:
    app: {{ .Values.mysql-instance-local.deployment.name }}