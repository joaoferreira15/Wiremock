apiVersion: apps/v1
kind: Deployment
metadata:
    name: {{ .Values.spring-instance-local.deployment.name }}
    labels:
        app: {{ .Values.spring-instance-local.deployment.name }}
spec:
    replicas: {{ .Values.spring-instance-local.replicaCount }}
    selector:
        matchLabels:
            app: {{ .Values.spring-instance-local.deployment.name }}
    strategy:
        type: RollingUpdate
    template:
        metadata:
            labels:
                app: {{ .Values.spring-instance-local.deployment.name }}
        spec:
            {{- if .Values.spring-instance-local.imagePullSecrets.enabled }}
            imagePullSecrets:
                - name: {{ .Values.spring-instance-local.imagePullSecrets.name }}
            {{- end }}
            containers:
                - name: {{ .Values.spring-instance-local.deployment.container.name }}
                  image: "{{ .Values.spring-instance-local.image.repository }}:{{ .Values.spring-instance-local.image.tag }}"
                  imagePullPolicy: {{ .Values.spring-instance-local.image.pullPolicy }}
                  ports:
                      - containerPort: {{ .Values.spring-instance-local.deployment.container.port }}
                  env:
                      - name: JAVA_HOME
                        valueFrom:
                        configMapKeyRef:
                            name: {{ .Values.spring-instance-local.configMap.name }}
                            key: JAVA_HOME
                  livenessProbe:
                      httpGet:
                          path: /actuator/health/liveness
                          port: {{ .Values.spring-instance-local.deployment.container.port }}
                      initialDelaySeconds: {{ .Values.spring-instance-local.probes.liveness.initialDelaySeconds }}
                      periodSeconds:  {{ .Values.spring-instance-local.probes.liveness.periodSeconds }}
                  readinessProbe:
                      httpGet:
                          path: /actuator/health/readiness
                          port: {{ .Values.spring-instance-local.deployment.container.port }}
                      initialDelaySeconds: {{ .Values.spring-instance-local.probes.liveness.initialDelaySeconds }}
                      periodSeconds:  {{ .Values.spring-instance-local.probes.liveness.periodSeconds }}
                  resources:
                      limits:
                          cpu: {{ .Values.spring-instance-local.resources.limits.cpu }}
                          memory: {{ .Values.spring-instance-local.resources.limits.memory }}
                      requests:
                          cpu: {{ .Values.spring-instance-local.resources.requests.cpu }}
                          memory: {{ .Values.spring-instance-local.resources.requests.memory}}
